repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
  rev: v0.9.9
  hooks:
    # Run the linter.
    - id: ruff
      args: [ --fix ]
      files: "server/py4web"
    # Run the formatter.
    - id: ruff-format
      files: "server/py4web"
