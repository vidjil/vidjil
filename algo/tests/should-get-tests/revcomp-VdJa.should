!LAUNCH: $VIDJIL_DIR/$EXEC -g $VIDJIL_DIR/germline/ -r 1 -c clones $VIDJIL_DATA/revcomp-VdJa.fa

$ Just one window found
1:==> found 1 windows

$ Just one clone found
1:==> 1 clones

$ Two reads detected on TRA+D
1:TRA.D -> 2

$ One clone for the two reads, Vd1/Ja29
1:clone-001--TRA[+]D--.*002-.* TRDV1[*]01 .* TRAJ29[*]01
