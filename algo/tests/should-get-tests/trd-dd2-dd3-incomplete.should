!LAUNCH: $VIDJIL_DIR/$EXEC -w 10 -e 20 --all -g $VIDJIL_DIR/germline $VIDJIL_DATA/trd-dd2-dd3.fa

$ Segment 8 reads
1:junction detected in 8 reads

$ 6 reads are detected on TRD+ (with -w 10, otherwise the reads are too short)
1: TRD[+] .*->.* 6

