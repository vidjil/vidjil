!LAUNCH: $VIDJIL_DIR/$EXEC -e 1e-5  -g $VIDJIL_DIR/germline -r 1 bug3009.fa

$ Segmented as TRD+
1: TRD\+ -> 1
1: SEG_\+ -> 1

$ VDJ assignation
1:TRDD2.01 1//2 TRDD3.01
# The correct deletion is not found as the updown stream region
# is not taken into account anymore when doing the alignment
