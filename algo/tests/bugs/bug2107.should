!LAUNCH: $VIDJIL_DIR/$EXEC $VIDJIL_DEFAULT_OPTIONS -e 1e-3 -r 1 -y 0 -z 0 -g $VIDJIL_DIR/germline/homo-sapiens.g ./bug2107.fa

# No sequences are segmented
1: junction detected in 0 reads

# Most sequences are not reported as 'only V', but as 'too few V/J'
1: UNSEG only V/.* 1
# see #4730
f1: UNSEG too few V/J .* 11
1: UNSEG too few V/J .* 9


