# Data used for issue #4407 & #4408

Data extracted from a run where the first samples were analysed with a config XXX, and the last samples with a config YYY.
An error appeared on order loading, with variable `order` and `stock_order` larger than the number of samples actually analysed with config YYY.
